import { Routes } from '@angular/router';
import { TestFirebaseComponent } from './test-firebase/test-firebase.component'; // Import the component

export const routes: Routes = [
  { path: 'test-firebase', component: TestFirebaseComponent } // Add the route
];
